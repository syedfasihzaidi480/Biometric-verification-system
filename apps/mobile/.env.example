# Mobile app env (copy to .env)
###############################################################
# REQUIRED: Point the app at your running web server (port 4000)
# If using Android emulator:
#   EXPO_PUBLIC_API_URL=http://10.0.2.2:4000
# If using a physical phone on the same Wi‑Fi:
#   Replace <LAN-IP> with your PC's IP (shown when you run `npm run dev` in apps/web)
#   EXPO_PUBLIC_API_URL=http://<LAN-IP>:4000
###############################################################

# Preferred var (api.js uses this first)
EXPO_PUBLIC_API_URL=

# Back-compat vars (kept for other modules that reference BASE_URL/PROXY)
EXPO_PUBLIC_BASE_URL=
EXPO_PUBLIC_PROXY_BASE_URL=
EXPO_PUBLIC_HOST=
EXPO_PUBLIC_PROJECT_GROUP_ID=create-anything-dev
